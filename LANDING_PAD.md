# owlbox Landing Pad

Generated from repository sources. It introduces no new authority or facts.

## Index

- `.editorconfig`
- `.gitattributes`
- `.github/workflows/validate.yml`
- `.gitignore`
- `HOWTOUPDATEOWL.md`
- `LEGACY.md`
- `MAP.md`
- `OUTLINE.md`
- `OWL.md`
- `README.md`
- `WISDOM.md`
- `control/owl.json`
- `scripts/owl.py`
- `templates/LEGACY.md`
- `templates/OUTLINE.md`
- `templates/WISDOM.md`
- `tests/test_owl.py`

## FILE: .editorconfig

```text
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true

[*.py]
indent_style = space
indent_size = 4

[*.{json,yml,yaml}]
indent_style = space
indent_size = 2
```

## FILE: .gitattributes

```text
* text=auto eol=lf
*.md text eol=lf
*.json text eol=lf
*.py text eol=lf
*.yml text eol=lf
```

## FILE: .github/workflows/validate.yml

```text
name: Validate owlbox

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python scripts/owl.py check .
      - run: python -m unittest discover -s tests
      - run: git diff --exit-code
```

## FILE: .gitignore

```text
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.DS_Store
Thumbs.db
*.tmp
*.bak
```

## FILE: HOWTOUPDATEOWL.md

```text
# How to Update owlbox

## First rule

Whenever any tracked source file changes, regenerate and verify `MAP.md` and `LANDING_PAD.md` before committing.

```bash
python scripts/owl.py generate .
python scripts/owl.py check .
python -m unittest discover -s tests
```

## Update procedure

1. Read `OUTLINE.md`, `WISDOM.md`, and `LEGACY.md`.
2. Inspect the files and evidence relevant to the change.
3. Edit canonical sources.
4. Update owlbox's own OWL files:
   - active state in `OUTLINE.md`;
   - durable lessons in `WISDOM.md`;
   - significant events in `LEGACY.md`.
5. Run generation, validation, and tests.
6. Review the complete diff.
7. Commit only after every gate passes.

## Adding a file

A new file must:

- have one distinct purpose;
- be declared in `control/owl.json`;
- appear in `MAP.md`;
- be included or deliberately excluded from `LANDING_PAD.md`;
- add more value than a section in an existing file.

## Changing the OWL contract

Update:

1. `OWL.md`;
2. `control/owl.json`;
3. templates;
4. validator tests;
5. generated views;
6. `LEGACY.md`.

## Release check

```bash
python scripts/owl.py check .
python -m unittest discover -s tests
git diff --check
git status --short
```

A release is not verified until the committed remote state is checked independently.
```

## FILE: LEGACY.md

```text
# Legacy

## 2026-07-13 — owlbox created

The repository was created as the reference implementation of the OWL Method, separating project continuity from assistant governance.

## 2026-07-13 — Version 1 authored

Version 1 defined `OUTLINE.md`, `WISDOM.md`, and `LEGACY.md`, then added templates, validation, tests, CI, a human-readable repository map, and an assistant landing pad.
```

## FILE: MAP.md

```text
# Repository Map

Generated by `python scripts/owl.py generate .`. Do not edit manually.

```text
.
├── .editorconfig
├── .gitattributes
├── .github
│   └── workflows
│       └── validate.yml
├── .gitignore
├── HOWTOUPDATEOWL.md
├── LANDING_PAD.md
├── LEGACY.md
├── MAP.md
├── OUTLINE.md
├── OWL.md
├── README.md
├── WISDOM.md
├── control
│   └── owl.json
├── scripts
│   └── owl.py
├── templates
│   ├── LEGACY.md
│   ├── OUTLINE.md
│   └── WISDOM.md
└── tests
    └── test_owl.py
```

## File purposes

- `.editorconfig` — **source.** Defines portable text formatting.
- `.gitattributes` — **source.** Normalizes text and line endings.
- `.github/workflows/validate.yml` — **source.** Runs owlbox validation and tests in CI.
- `.gitignore` — **source.** Excludes caches and temporary files.
- `HOWTOUPDATEOWL.md` — **source.** Defines the safe update and release procedure.
- `LANDING_PAD.md` — **generated.** Generated assistant-readable repository bundle.
- `LEGACY.md` — **source.** Records owlbox's chronological continuity.
- `MAP.md` — **generated.** Generated human-readable repository map.
- `OUTLINE.md` — **source.** Tracks owlbox's active work.
- `OWL.md` — **source.** Defines the OWL Method.
- `README.md` — **source.** Introduces owlbox and its shortest usable path.
- `WISDOM.md` — **source.** Stores durable lessons learned while building owlbox.
- `control/owl.json` — **source.** Defines repository files, generated views, and validation inputs.
- `scripts/owl.py` — **source.** Initializes, generates, and validates OWL repositories.
- `templates/LEGACY.md` — **source.** Starter chronological project record.
- `templates/OUTLINE.md` — **source.** Starter active working-memory file.
- `templates/WISDOM.md` — **source.** Starter durable-knowledge file.
- `tests/test_owl.py` — **source.** Tests success and known failure modes.
```

## FILE: OUTLINE.md

```text
# Outline

## Current objective

Publish owlbox version 1 as a compact, self-validating implementation of the OWL Method.

## Verified current state

- The specification, templates, validator, tests, CI, map, and landing pad are authored.
- Local validation must pass before publication.

## Active work

- Run the complete validation chain.
- Review the generated files and repository diff.
- Publish the first complete version.

## Next action

Run `python scripts/owl.py check .` and `python -m unittest`.

## Unresolved

- Licensing remains unchanged until The User chooses terms.
```

## FILE: OWL.md

```text
# The OWL Method

## Purpose

OWL grounds The Assistant in a project through:

- `OUTLINE.md` — active working memory;
- `WISDOM.md` — durable project knowledge;
- `LEGACY.md` — chronological continuity.

The User owns the project. The Assistant maintains OWL subject to The User's direction and the project's governing rules.

## Continuity standard

A fresh Assistant passes an OWL handoff when the repository alone reveals:

1. the current objective;
2. the verified current state;
3. the immediate next action;
4. unresolved questions, risks, and blockers;
5. durable lessons;
6. major decisions and milestones that explain the present.

Missing chat history must not prevent safe continuation.

## Outline

`OUTLINE.md` is The Assistant's desk.

It contains:

- current objective;
- verified current state;
- active work;
- next action;
- unresolved items;
- temporary notes that still affect the work.

It is human-readable, actively rewritten, aggressively culled, and expected to trend toward zero.

It is not a transcript, an append-only log, a permanent lesson store, or a substitute for evidence.

## Wisdom

`WISDOM.md` is the project's curated long-term knowledge.

It contains proven methods, verified discoveries, recurring failure modes, durable principles, and corrections future Assistants should not have to rediscover.

It grows slowly. It may be revised when better evidence narrows or disproves a lesson. It does not automatically change project governance.

## Legacy

`LEGACY.md` explains how the project reached its present state.

It records dated milestones, decisions, state transitions, significant failures and recoveries, and releases.

It is chronological and append-oriented. Facts may be corrected, but meaningful history is not silently rewritten.

## Information flow

```text
Active and temporary → OUTLINE.md
Proven and reusable  → WISDOM.md
Significant event    → LEGACY.md
Finished and trivial → remove
```

One fact may appear in more than one OWL file only when each entry serves a different purpose.

## Grounding procedure

Before substantial work, The Assistant:

1. reads all three OWL files;
2. checks relevant live evidence and repository state;
3. identifies conflicts, stale claims, and unknowns;
4. corrects Outline before acting when necessary;
5. acts from reconciled state, not remembered conversation.

After substantial work, The Assistant:

1. verifies the result;
2. updates Outline;
3. promotes durable lessons to Wisdom;
4. records significant events in Legacy;
5. runs the OWL check.

## Evidence language

- **Verified** — supported by current direct evidence.
- **Inferred** — reasoned from stated evidence.
- **Unknown** — not established.

Do not use a stronger state than the evidence supports.

## Failure conditions

An OWL handoff fails when:

- the current objective or next action cannot be found;
- Outline presents stale work as current;
- Wisdom contains untested guesses;
- Legacy omits a state transition needed to understand the present;
- the files contradict one another without reconciliation;
- generated views are stale;
- validation is claimed without reproducible evidence.

## Recovery

When continuity breaks:

1. stop consequential work;
2. inspect live repository and system evidence;
3. identify the last verified state;
4. correct Outline first;
5. repair Wisdom and Legacy only where evidence supports it;
6. run validation;
7. resume from the reconciled state.
```

## FILE: README.md

```text
# owlbox

**The reference implementation of the OWL Method: Outline, Wisdom, Legacy.**

> **OWL is not memory. It is a memory container.**
>
> The project's continuity lives inside the box. A fresh Assistant should be able to open it, inspect it, verify it, and continue the work.

owlbox keeps project continuity in three human-readable files:

| File | Purpose |
|---|---|
| `OUTLINE.md` | Current objective, verified state, active work, next action, unresolved items |
| `WISDOM.md` | Durable lessons future Assistants should not have to rediscover |
| `LEGACY.md` | Chronological milestones, decisions, failures, recoveries, and releases |

## Use owlbox in a project

```bash
python path/to/owlbox/scripts/owl.py init .
python path/to/owlbox/scripts/owl.py check .
```

Then maintain the three OWL files as the project changes.

## Validate owlbox itself

```bash
python scripts/owl.py check .
python -m unittest discover -s tests
```

## Reading path

- **30 seconds:** this file
- **5 minutes:** [`OWL.md`](OWL.md)
- **15 minutes:** [`HOWTOUPDATEOWL.md`](HOWTOUPDATEOWL.md)
- **Everything:** [`LANDING_PAD.md`](LANDING_PAD.md)

See [`MAP.md`](MAP.md) for every tracked file and its purpose.
```

## FILE: WISDOM.md

```text
# Wisdom

## Repository memory beats conversation memory

A project survives session loss, assistant replacement, and long pauses when its repository carries current state, durable lessons, and meaningful chronology.

## Separate memory by purpose

Current work, durable knowledge, and chronology decay differently. Keeping them in Outline, Wisdom, and Legacy prevents one overloaded document from becoming stale or unreadable.

## Continuity must be testable

A handoff does not succeed because The Assistant says it understands. It succeeds when the required state can be recovered from the repository and checked.

## Preserve distinct evidence paths

Human-readable memory, structured control, generated views, and tests should agree while remaining capable of exposing different failures.
```

## FILE: control/owl.json

```text
{
  "name": "owlbox",
  "method": "The OWL Method",
  "version": "1.0.0",
  "canonical_files": [
    "README.md",
    "OWL.md",
    "HOWTOUPDATEOWL.md",
    "OUTLINE.md",
    "WISDOM.md",
    "LEGACY.md",
    "control/owl.json",
    "templates/OUTLINE.md",
    "templates/WISDOM.md",
    "templates/LEGACY.md",
    "scripts/owl.py",
    "tests/test_owl.py",
    ".github/workflows/validate.yml",
    ".editorconfig",
    ".gitattributes",
    ".gitignore"
  ],
  "generated_files": [
    "MAP.md",
    "LANDING_PAD.md"
  ],
  "landing_pad_exclude": [
    "LANDING_PAD.md"
  ],
  "file_summaries": {
    ".editorconfig": "Defines portable text formatting.",
    ".gitattributes": "Normalizes text and line endings.",
    ".github/workflows/validate.yml": "Runs owlbox validation and tests in CI.",
    ".gitignore": "Excludes caches and temporary files.",
    "HOWTOUPDATEOWL.md": "Defines the safe update and release procedure.",
    "LANDING_PAD.md": "Generated assistant-readable repository bundle.",
    "LEGACY.md": "Records owlbox's chronological continuity.",
    "MAP.md": "Generated human-readable repository map.",
    "OUTLINE.md": "Tracks owlbox's active work.",
    "OWL.md": "Defines the OWL Method.",
    "README.md": "Introduces owlbox and its shortest usable path.",
    "WISDOM.md": "Stores durable lessons learned while building owlbox.",
    "control/owl.json": "Defines repository files, generated views, and validation inputs.",
    "scripts/owl.py": "Initializes, generates, and validates OWL repositories.",
    "templates/LEGACY.md": "Starter chronological project record.",
    "templates/OUTLINE.md": "Starter active working-memory file.",
    "templates/WISDOM.md": "Starter durable-knowledge file.",
    "tests/test_owl.py": "Tests success and known failure modes."
  }
}
```

## FILE: scripts/owl.py

```text
#!/usr/bin/env python3
"""Initialize, generate, and validate OWL project memory."""

from __future__ import annotations

import argparse
import json
import shutil
import sys
from pathlib import Path
from typing import Iterable

OWL_FILES = ("OUTLINE.md", "WISDOM.md", "LEGACY.md")
EXCLUDED_PARTS = {".git", "__pycache__", ".venv", ".pytest_cache"}
TEXT_SUFFIXES = {".md", ".json", ".py", ".yml", ".yaml", ".txt", ""}
SELF_MARKER = Path("control/owl.json")


class OwlError(Exception):
    pass


def fail(message: str) -> None:
    raise OwlError(message)


def read_text(path: Path) -> str:
    try:
        raw = path.read_bytes()
    except FileNotFoundError:
        fail(f"missing required file: {path}")
    if b"\r" in raw:
        fail(f"{path}: line endings must be LF")
    try:
        text = raw.decode("utf-8")
    except UnicodeDecodeError:
        fail(f"{path}: file must be UTF-8 text")
    if text and not text.endswith("\n"):
        fail(f"{path}: file must end with a newline")
    return text


def section_names(text: str) -> set[str]:
    return {line[3:].strip() for line in text.splitlines() if line.startswith("## ")}


def section_body(text: str, name: str) -> str:
    marker = f"## {name}\n"
    if marker not in text:
        return ""
    return text.split(marker, 1)[1].split("\n## ", 1)[0].strip()


def validate_project(root: Path) -> list[str]:
    messages: list[str] = []
    for name in OWL_FILES:
        text = read_text(root / name)
        if not text.startswith("# "):
            fail(f"{name}: first line must be a level-one heading")
        messages.append(f"PASS: {name} exists and is readable")

    outline = read_text(root / "OUTLINE.md")
    required = {
        "Current objective",
        "Verified current state",
        "Active work",
        "Next action",
        "Unresolved",
    }
    missing = sorted(required - section_names(outline))
    if missing:
        fail(f"OUTLINE.md: missing sections: {', '.join(missing)}")

    for name in ("Current objective", "Verified current state", "Next action"):
        if not section_body(outline, name):
            fail(f"OUTLINE.md: '{name}' must not be empty")

    messages.append("PASS: OUTLINE.md contains the continuity handoff fields")
    return messages


def load_manifest(root: Path) -> dict:
    path = root / SELF_MARKER
    try:
        data = json.loads(read_text(path))
    except json.JSONDecodeError as exc:
        fail(f"{path}: invalid JSON: {exc}")
    required = {"name", "version", "canonical_files", "generated_files", "file_summaries"}
    missing = sorted(required - data.keys())
    if missing:
        fail(f"{path}: missing keys: {', '.join(missing)}")
    return data


def verify_file_set(root: Path, manifest: dict) -> list[str]:
    declared = set(manifest["canonical_files"]) | set(manifest["generated_files"])
    actual: set[str] = set()

    for path in root.rglob("*"):
        if not path.is_file():
            continue
        if any(part in EXCLUDED_PARTS for part in path.parts):
            continue
        actual.add(path.relative_to(root).as_posix())

    missing = sorted(declared - actual)
    unexpected = sorted(actual - declared)
    if missing:
        fail("manifest declares missing files: " + ", ".join(missing))
    if unexpected:
        fail("unclassified files: " + ", ".join(unexpected))
    return sorted(declared)


def make_map(manifest: dict) -> str:
    paths = sorted(set(manifest["canonical_files"]) | set(manifest["generated_files"]))
    tree: dict = {}
    for rel in paths:
        node = tree
        for part in rel.split("/"):
            node = node.setdefault(part, {})

    lines = [
        "# Repository Map",
        "",
        "Generated by `python scripts/owl.py generate .`. Do not edit manually.",
        "",
        "```text",
        ".",
    ]

    def walk(node: dict, prefix: str = "") -> None:
        names = sorted(node)
        for index, name in enumerate(names):
            last = index == len(names) - 1
            lines.append(f"{prefix}{'└── ' if last else '├── '}{name}")
            if node[name]:
                walk(node[name], prefix + ("    " if last else "│   "))

    walk(tree)
    lines += ["```", "", "## File purposes", ""]

    for rel in paths:
        summary = manifest["file_summaries"].get(rel)
        if not summary:
            fail(f"control/owl.json: missing file summary for {rel}")
        role = "generated" if rel in manifest["generated_files"] else "source"
        lines.append(f"- `{rel}` — **{role}.** {summary}")

    return "\n".join(lines) + "\n"


def make_landing_pad(root: Path, manifest: dict) -> str:
    excluded = set(manifest.get("landing_pad_exclude", []))
    included = [
        rel for rel in sorted(manifest["canonical_files"] + manifest["generated_files"])
        if rel not in excluded
    ]

    lines = [
        "# owlbox Landing Pad",
        "",
        "Generated from repository sources. It introduces no new authority or facts.",
        "",
        "## Index",
        "",
        *[f"- `{rel}`" for rel in included],
        "",
    ]

    for rel in included:
        text = make_map(manifest) if rel == "MAP.md" else read_text(root / rel)
        lines += [f"## FILE: {rel}", "", "```text", text.rstrip("\n"), "```", ""]

    return "\n".join(lines)


def generate(root: Path) -> list[str]:
    manifest = load_manifest(root)
    (root / "MAP.md").write_text(make_map(manifest), encoding="utf-8", newline="\n")
    (root / "LANDING_PAD.md").write_text(
        make_landing_pad(root, manifest), encoding="utf-8", newline="\n"
    )
    return ["PASS: MAP.md generated", "PASS: LANDING_PAD.md generated"]


def validate_self(root: Path) -> list[str]:
    manifest = load_manifest(root)
    messages = validate_project(root)
    paths = verify_file_set(root, manifest)

    for rel in paths:
        path = root / rel
        if path.suffix in TEXT_SUFFIXES:
            read_text(path)
    messages.append("PASS: repository file set matches control/owl.json")

    if read_text(root / "MAP.md") != make_map(manifest):
        fail("MAP.md is stale; run: python scripts/owl.py generate .")
    if read_text(root / "LANDING_PAD.md") != make_landing_pad(root, manifest):
        fail("LANDING_PAD.md is stale; run: python scripts/owl.py generate .")
    messages.append("PASS: generated views match repository sources")

    banned = tuple("".join(map(chr, codes)) for codes in ((67,104,97,116,71,80,84),(67,108,97,117,100,101),(71,80,84)))
    for rel in manifest["canonical_files"]:
        text = read_text(root / rel)
        for term in banned:
            if term.casefold() in text.casefold():
                fail(f"{rel}: vendor or model-specific term is not allowed: {term}")
    messages.append("PASS: canonical sources are vendor-neutral")
    return messages


def init_project(root: Path, force: bool) -> list[str]:
    template_root = Path(__file__).resolve().parent.parent / "templates"
    root.mkdir(parents=True, exist_ok=True)
    created = []

    for name in OWL_FILES:
        target = root / name
        if target.exists() and not force:
            continue
        shutil.copyfile(template_root / name, target)
        created.append(name)

    return [
        "PASS: created " + ", ".join(created)
        if created
        else "PASS: OWL files already exist; nothing changed"
    ]


def print_messages(messages: Iterable[str]) -> None:
    for message in messages:
        print(message)


def main(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    commands = parser.add_subparsers(dest="command", required=True)

    init_cmd = commands.add_parser("init")
    init_cmd.add_argument("path", nargs="?", default=".")
    init_cmd.add_argument("--force", action="store_true")

    for name in ("generate", "validate", "check"):
        cmd = commands.add_parser(name)
        cmd.add_argument("path", nargs="?", default=".")

    args = parser.parse_args(argv)
    root = Path(args.path).resolve()

    try:
        if args.command == "init":
            messages = init_project(root, args.force)
        elif args.command == "generate":
            if not (root / SELF_MARKER).exists():
                fail("generate applies only to the owlbox source repository")
            messages = generate(root)
        elif args.command == "validate":
            messages = validate_self(root) if (root / SELF_MARKER).exists() else validate_project(root)
        elif args.command == "check":
            messages = (
                generate(root) + validate_self(root)
                if (root / SELF_MARKER).exists()
                else validate_project(root)
            )
        else:
            fail(f"unknown command: {args.command}")

        print_messages(messages)
        print("OWL CHECK PASSED")
        return 0
    except OwlError as exc:
        print(f"OWL CHECK FAILED: {exc}", file=sys.stderr)
        return 1


if __name__ == "__main__":
    raise SystemExit(main())
```

## FILE: templates/LEGACY.md

```text
# Legacy

## YYYY-MM-DD — Event

Record a significant milestone, decision, failure, recovery, release, or state transition.

State what changed and why it matters. Name supporting evidence when practical.
```

## FILE: templates/OUTLINE.md

```text
# Outline

## Current objective

State the single current objective.

## Verified current state

- Record only current facts supported by evidence.

## Active work

- List work actually in progress.

## Next action

State the next concrete action.

## Unresolved

- List blockers, risks, questions, and important unknowns.
```

## FILE: templates/WISDOM.md

```text
# Wisdom

Add concise, evidence-grounded lessons future Assistants should not have to rediscover.

Each lesson should state:

- the durable lesson;
- its supporting evidence or experience;
- its limits when those limits matter.
```

## FILE: tests/test_owl.py

```text
import shutil
import tempfile
import unittest
from pathlib import Path

from scripts import owl


class OwlProjectTests(unittest.TestCase):
    def setUp(self):
        self.tmp = tempfile.TemporaryDirectory()
        self.root = Path(self.tmp.name)
        templates = Path(__file__).resolve().parents[1] / "templates"
        for name in owl.OWL_FILES:
            shutil.copyfile(templates / name, self.root / name)

    def tearDown(self):
        self.tmp.cleanup()

    def test_valid_project(self):
        messages = owl.validate_project(self.root)
        self.assertTrue(any("continuity" in item for item in messages))

    def test_missing_file_fails(self):
        (self.root / "WISDOM.md").unlink()
        with self.assertRaises(owl.OwlError):
            owl.validate_project(self.root)

    def test_missing_outline_section_fails(self):
        path = self.root / "OUTLINE.md"
        text = path.read_text(encoding="utf-8").replace("## Next action", "## Later")
        path.write_text(text, encoding="utf-8")
        with self.assertRaises(owl.OwlError):
            owl.validate_project(self.root)

    def test_empty_objective_fails(self):
        path = self.root / "OUTLINE.md"
        text = path.read_text(encoding="utf-8")
        start = text.index("## Current objective")
        end = text.index("## Verified current state")
        path.write_text(text[:start] + "## Current objective\n\n" + text[end:], encoding="utf-8")
        with self.assertRaises(owl.OwlError):
            owl.validate_project(self.root)


class OwlboxTests(unittest.TestCase):
    def test_self_check(self):
        root = Path(__file__).resolve().parents[1]
        owl.generate(root)
        messages = owl.validate_self(root)
        self.assertTrue(any("generated views" in item for item in messages))


if __name__ == "__main__":
    unittest.main()
```
